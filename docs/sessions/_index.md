# Session Index

| Date | Summary |
|------|---------|
| 2026-04-20 | Project scaffolded & named — "Avid MXF Peek" (`com.lucesumbrarum.AvidMXFPeek`). Directions installed, folder structure created, all v1 scoping questions resolved (single-folder UX, CSV+JSON export in v1, `.mdb` parsing as parallel spike). No code yet. |
| 2026-04-20-pm | Research pass on `mxf2raw --info` (✅ viable) and `.mdb`/`.pmr` (❌ OMFI/Bento — orphan detection deferred to v1.1). Wrote `IMPLEMENTATION_PLAN.md`. **Waves 1–3 complete in one session:** cloned P2toMXF.xcodeproj + retrofitted App Shell Standard (Wave 1); added `MXFHeaderInfo` + `BMXWrapper.info(url:)` XML parser + `MXFFolderScanner` AsyncStream (Wave 2); added `Clip` + `ClipAggregator` + `AuditReportExporter` (CSV/JSON v1.0.0) + `docs/specs/audit-report-schema.md` (Wave 3). All builds clean, zero warnings, app launches. Project at 60%. |
| 2026-04-20-evening | Code-reviewed Waves 1–3 (clean security, 2 medium issues logged: subprocess-pipe tail race + scan-cancel subprocess leak). **Wave 4 complete start-to-finish:** `ScanState` enum + `@Observable ScanModel` with A1 state shape + B3 debounced re-aggregation (50 yields ∥ 250 ms); 6-column `ClipTableView` + `ClipInspectorView`; toolbar with `.fileImporter`, Rescan, `NSSavePanel` CSV/JSON export; `.dropDestination` folder target with accent-bordered hover overlay; `TimelineView(.periodic)` live elapsed-time readout. Five clean builds through the wave; app boots and idle pane renders. Wave 4.6 pbxproj sweep deferred (no new file slots needed yet). Runtime scan→export unverified — gated on user-supplied MXF samples (Wave 5.2). Also captured collaboration feedback: don't scaffold TODO "contributions" for mechanical code with no remaining design choice. Project at 80%. |
| 2026-04-20-night | Real-data validation exposed a showstopper: `mxf2raw` fails deterministically on every Avid DNxHD video stem (`libMXF` hard-asserts `item->length == 16` — Avid's progressive VideoLineMap violates it; `bbc/bmx` archived Sep 2025, `ebu/bmx` has no fix). **Pivot to `ffprobe`** executed via `docs/plans/2026-04-20-ffprobe-pivot.md` (decision `2026-04-20-C`): new `FFProbeMapper`, `runAndCollect` retires code-review #1 tail-race as a side effect, `project_name` now reliably surfaced. Plus per-file track-count fix (OP-Atom stems encode the whole clip graph in `streams` — count only own essence). **Wave 4.6-A** bundle cleanup: removed ffmpeg/mxf2raw/bmxtranswrap/lib from pbxproj + disk (122→59 MB in Resources). **Wave 4.7** code-review nits fixed + CSV schema doc expanded. **Wave 5.0** test target (user handled via Xcode File→New→Target). **Wave 5.1**: 26 unit tests all green — `FFProbeMapperTests`, `ClipAggregatorTests`, `AuditReportExporterTests`. **Wave 4.6-B** dead-code sweep: 30 P2toMXF files deleted via sed-on-pbxproj + disk rm, 5 files trimmed in-place (P2CardParser 613→242, ReportGenerator 401→199, BMXWrapper 650→300, BundledToolResolver + AvidMXFPeekApp pruned). Source ~4500→1507 LOC. Clean `.app` 74 MB (ffprobe 59 + Assets.car 11 + Sparkle 2.6 + exe 1.4). v1 feature-complete + test-covered. Remaining: P5 script cleanup, 5.2b multi-project stress, 5.3 adversarial. |
| 2026-04-21 | Huge day — v1 feature-complete, public on GitHub, v1.2 player spec drafted. **AM: cookbook capture** of patterns 43 + 44 (subprocess-fire-and-collect, inherited-project-dead-code-sweep). **PM: P5 bundle-script cleanup** — `bundle-ffprobe.sh` replaces `bundle-ffmpeg.sh` with correct `01_Project/…` path, otool-based dylib preflight rejecting Homebrew ffprobe, BSD-portable same-file copy guard; `sign-bundled-binaries.sh` stripped to ffprobe-only (80→55 LOC). **Evening: 5.3 cases 1–3** — new `MXFFolderScannerTests.swift` with 8 tests across empty folder / corrupt MXF / permission-denied axes; uncovered macOS firmlink gotcha during authoring (`/var` vs `/private/var` — `resolvingSymlinksInPath()` is no-op on APFS firmlinks, `canonicalPath` resolves them), promoted as cookbook pattern 45 `macos-firmlink-canonical-path`. **5.2b verified** against real 3-project data — Table showed 3 distinct `project_name` values (`sss`, `PEEKY`, `Fookls`), per-clip attribution correct. **UI bug fixed** — rows bleeding above sticky column header at scroll bottom; root cause = asymmetric `.scanning` (VStack-wrapped) vs `.complete` (bare Table) branches in `ScanMainView` under `.windowStyle(.hiddenTitleBar)`. Applied VStack+Divider symmetry + `.clipped()` defense-in-depth. **Git init + first commit + public push** to github.com/Xpycode/MXF-Peek — 235 files, ~39.7k lines, commit `de545b0`; repo name drops "Avid" to dodge trademark, app-facing name unchanged. **v1.2 player spec `docs/specs/player.md` drafted** after research spike: ffprobe'd real stems (confirms DNxHD + PCM mono), swift-probed AVFoundation on raw MXF (**BLOCKER — `AVURLAsset Code=-11828 "Cannot Open"` on both video + audio, macOS has no MXF format reader, original AVComposition plan DOA**), benchmarked ffmpeg (-c copy remux = 15s/11GB for 13-min clip; DNxHR LB proxy = ~40s/3.6GB). Spec stays Draft; Wave P1 spike (4h) scoped to resolve 4 open questions (AVPlayer-on-.mov vs isPlayable=false, AVMediaSelectionGroup audio switching, proxy codec shootout, stereo-pair semantics) before `/plan`. Decisions: MXF-Peek repo name; preview-is-the-Kyno-replacement-feature; ffmpeg reintroduction required (74→165 MB .app); spec stays draft until P1. **Night: Windows port exploration** — surveyed framework landscape (WPF recommended), confirmed self-contained .NET publish solves the runtime-install problem, deep agent-based codebase analysis confirmed clean logic/platform separation, estimated ~5 working days for feature-parity Windows port. Player deferred until macOS solution found first; LibVLCSharp flagged as potentially easier path on Windows (handles MXF natively, no transcode). Decision: separate C# / WPF app when the time comes. **Late: player design revision + implementation plan** — user pushed back on cached-transcode's linear-in-clip-length startup delay and on the semantically-broken UNC-path fast-path. Did ultrathink research pass on 7 alternative approaches ([HLS+file://](https://developer.apple.com/forums/thread/69357) doesn't work; [AVAssetResourceLoaderDelegate](https://developer.apple.com/forums/thread/113063) can't intercept HLS segments; [KSPlayer](https://github.com/kingslay/KSPlayer) GPL-blocked; [VLCKit input-slave](https://forum.videolan.org/viewtopic.php?t=73488) broken). **Chose Option 2: HLS live-mux + localhost loopback HTTP server + AVPlayer.** Two probes verified: (a) ffmpeg HLS command against real MXF works — `h264_videotoolbox -b:v 4M` encode runs at ~9× realtime so first segment lands in <1s wall time, enabling uniform ~0.5–2s startup regardless of clip length; (b) NWListener loopback-only binding via `requiredLocalEndpoint = .hostPort(.ipv4(.loopback), .any)` is user-invisible (no firewall/Local-Network prompts). Wrote `docs/plans/2026-04-22-player-hls.md` — 8 waves (P1 spike timeboxed 4h → P2 bundling → P3 server → P4 transcoder → P5 cache → P6 coordinator → P7 UI → P8 adversarial), ~1,620 new Swift LOC + 90 MB bundled ffmpeg, ~18-22h total after spike. Plan stays Draft until P1 spike confirms end-to-end. Design decisions: Option 2 over 1 (startup latency), H.264/VT over DNxHR LB (smaller output, universally playable), HLS `event+append_list` for live-mux, UNC-path dropped entirely. |
| 2026-04-25 | **Waves P6 → P8.4 shipped — v1.2 player code-complete.** 79 → **92 tests** (0 fail), build clean (0 errors, 0 warnings). **P6:** `PlaybackState.swift` + `PlaybackCoordinator.swift` (~373 LOC). Researched `withObservationTracking`/`AVPlayerItem.status`/`AVMediaSelectionGroup` against Apple docs before coding — 4 plan corrections appended as §11 (re-arm requirement, Task supersedence, status observer, `NSViewRepresentable(AVPlayerView)` rationale). `selectedClipID` lifted from ContentView `@State` onto ScanModel; `@Bindable var model = model` body shadowing pattern bridges back. Transcoder injected as closure typealias for testability. 8 tests. **P7:** `PlayerView.swift` ~136 LOC — `showsTimecodes` + frame-stepping + JKL non-negotiable for editorial; phase overlays via `@ViewBuilder` switch; audio-pair Picker hidden when <2 pairs. `AvidMXFPeekApp.swift` rewritten to bootstrap coordinator graph in `.task` with graceful fallback. ClipInspectorView refactored: PlayerView + Divider + ScrollView(metadataGrid). Inspector toggle OFF clears selection → coordinator tears down. **P8.1:** 4 adversarial tests — video-only clip / odd-audio mono pair / disk-preflight failure / 5-rapid-selection stress. Caught one parallel-test flake on existing `rapidSelectionChangesCancelPriorTranscode`, watching it. **P8.4:** Researched macOS process-lifecycle (no Darwin `PR_SET_PDEATHSIG`; `.terminateLater` is canonical async-cleanup hook; ffmpeg `EXT-X-ENDLIST` flush 10-50 ms so 500 ms grace = 10× headroom). `AppDelegate.swift` ~30 LOC via `@NSApplicationDelegateAdaptor`; coordinator gained `activePIDs: Set<pid_t>` + `shutdown()` async (SIGTERM all → 500 ms → SIGKILL → server.stop). 1 real-subprocess test (`/bin/sleep 30` + assert `isRunning == false` after shutdown). Smoke launch + Cmd-Q verified, no orphan processes. **Lessons:** SourceKit lies during multi-file additions — trust `xcodebuild`. Plan corrections as numbered §10/§11 sections preserve design evolution. Real-subprocess tests beat mocks for OS-contract verification. **Remaining v1.2:** §P7.4 manual QA + §P8.3 Instruments memory profile (both need user + real MXF). Checklist at `docs/qa/v1.2-player-manual-qa.md`. |
| 2026-04-22 | **Waves P1 → P5 shipped — v1.2 player back-end complete.** 6 commits, +~2,800 LOC, 34 → **79 tests** (0 fail), .app 74 → 133 MB. **P1 spike** against the real 14-min Brutalist Dresser DNxHD clip: all 9 gating tasks green, end-to-end latency **2.5 s** (target <5), HLS live-mux + audio-pair switching + loopback HTTP validated empirically. **§10 post-spike corrections** from apple-docs MCP + RFC 7233 + HLS forums: 5 plan drifts corrected (ffmpeg bundle via shell-script build phase not sync folder; Range support **required** not optional; cache math 30 MB/min not 15; firstSegmentReady via 100 ms poll not DispatchSource; new first-play-seek-past-horizon risk). **P2:** `bundle-ffprobe.sh` → `bundle-toolchain.sh` (multi-binary), sign-bundled-binaries.sh loops over BINARIES array, pbxproj shell phase second `ditto`, `BundledTool.ffmpeg`. .app lands at 133 MB (projection was 134). **P3:** `PreviewHTTPServer.swift` ~306 LOC actor over NWListener, loopback-only, 206/416/501 Range handling, nonisolated connection serving so parallel GETs don't serialize. 11 tests (start/stop/idempotent, 404, 403, MIME dispatch, Range, concurrent). **P4:** `PreviewTranscoder.swift` ~260 LOC — struct with single AsyncStream-returning `transcode()`, raw Process + readabilityHandler progress parse, onTermination → terminate() → SIGKILL after 2 s; `AudioPair.swift` ~70 LOC `pairsFromClip(_:)` with sorted-by-filename + mono-fallback. 18 tests. **P5:** `PreviewCache.swift` ~250 LOC actor — SHA-256 16-char hash key, `.transcode-state` JSON sidecar, LRU via `accessedAt`, `.running` immune to eviction, 1 GB disk preflight. 14 tests. **Bug caught:** `URL.appendingPathComponent(_:)` without explicit `isDirectory:` does fs-probe — `hasDirectoryPath` flag flips between calls as the dir gets created, breaks URL equality across `prepareOutputDir` invocations. Cookbook-worthy. **Eviction test flakiness:** parallel-test Task.sleep timestamp races → rewrote to write `.transcode-state` directly with explicit base-date offsets. Commits: `92453f6` (P1) → `4c2e8f7` (§10 corrections) → `df079f7` (P2) → `37a7bb5` (P3) → `a2f8706` (P4) → `62eb143` (P5). Next: Wave P6 — `PlaybackCoordinator` + `PlaybackState` — the integration layer, harder to unit-test because of AVFoundation; then P7 `PlayerView` (needs custom audio-pair Picker per P1.6 AVPlayerView-HUD-won't-surface finding), then P8 adversarial + 30-min seek-past-horizon manual QA. |
