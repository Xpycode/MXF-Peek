# Session Index

| Date | Summary |
|------|---------|
| 2026-04-20 | Project scaffolded & named — "Avid MXF Peek" (`com.lucesumbrarum.AvidMXFPeek`). Directions installed, folder structure created, all v1 scoping questions resolved (single-folder UX, CSV+JSON export in v1, `.mdb` parsing as parallel spike). No code yet. |
| 2026-04-20-pm | Research pass on `mxf2raw --info` (✅ viable) and `.mdb`/`.pmr` (❌ OMFI/Bento — orphan detection deferred to v1.1). Wrote `IMPLEMENTATION_PLAN.md`. **Waves 1–3 complete in one session:** cloned P2toMXF.xcodeproj + retrofitted App Shell Standard (Wave 1); added `MXFHeaderInfo` + `BMXWrapper.info(url:)` XML parser + `MXFFolderScanner` AsyncStream (Wave 2); added `Clip` + `ClipAggregator` + `AuditReportExporter` (CSV/JSON v1.0.0) + `docs/specs/audit-report-schema.md` (Wave 3). All builds clean, zero warnings, app launches. Project at 60%. |
| 2026-04-20-evening | Code-reviewed Waves 1–3 (clean security, 2 medium issues logged: subprocess-pipe tail race + scan-cancel subprocess leak). **Wave 4 complete start-to-finish:** `ScanState` enum + `@Observable ScanModel` with A1 state shape + B3 debounced re-aggregation (50 yields ∥ 250 ms); 6-column `ClipTableView` + `ClipInspectorView`; toolbar with `.fileImporter`, Rescan, `NSSavePanel` CSV/JSON export; `.dropDestination` folder target with accent-bordered hover overlay; `TimelineView(.periodic)` live elapsed-time readout. Five clean builds through the wave; app boots and idle pane renders. Wave 4.6 pbxproj sweep deferred (no new file slots needed yet). Runtime scan→export unverified — gated on user-supplied MXF samples (Wave 5.2). Also captured collaboration feedback: don't scaffold TODO "contributions" for mechanical code with no remaining design choice. Project at 80%. |
| 2026-04-20-night | Real-data validation exposed a showstopper: `mxf2raw` fails deterministically on every Avid DNxHD video stem (`libMXF` hard-asserts `item->length == 16` — Avid's progressive VideoLineMap violates it; `bbc/bmx` archived Sep 2025, `ebu/bmx` has no fix). **Pivot to `ffprobe`** executed via `docs/plans/2026-04-20-ffprobe-pivot.md` (decision `2026-04-20-C`): new `FFProbeMapper`, `runAndCollect` retires code-review #1 tail-race as a side effect, `project_name` now reliably surfaced. Plus per-file track-count fix (OP-Atom stems encode the whole clip graph in `streams` — count only own essence). **Wave 4.6-A** bundle cleanup: removed ffmpeg/mxf2raw/bmxtranswrap/lib from pbxproj + disk (122→59 MB in Resources). **Wave 4.7** code-review nits fixed + CSV schema doc expanded. **Wave 5.0** test target (user handled via Xcode File→New→Target). **Wave 5.1**: 26 unit tests all green — `FFProbeMapperTests`, `ClipAggregatorTests`, `AuditReportExporterTests`. **Wave 4.6-B** dead-code sweep: 30 P2toMXF files deleted via sed-on-pbxproj + disk rm, 5 files trimmed in-place (P2CardParser 613→242, ReportGenerator 401→199, BMXWrapper 650→300, BundledToolResolver + AvidMXFPeekApp pruned). Source ~4500→1507 LOC. Clean `.app` 74 MB (ffprobe 59 + Assets.car 11 + Sparkle 2.6 + exe 1.4). v1 feature-complete + test-covered. Remaining: P5 script cleanup, 5.2b multi-project stress, 5.3 adversarial. |
| 2026-04-21 | Two-part day. **AM: cookbook capture** — patterns `43-subprocess-fire-and-collect.md` (waitUntilExit + readDataToEndOfFile inside Task.detached, retiring readabilityHandler tail-byte race) + `44-inherited-project-dead-code-sweep.md` (sed-on-pbxproj bulk deletion + disk rm + in-place trim + tests as safety net) promoted to master cookbook and synced to project. Global `~/.claude/CLAUDE.md` trigger keywords not added (same approval gate as 34/35). **PM: P5 bundle-script cleanup** — created `bundle-ffprobe.sh` (replaces `bundle-ffmpeg.sh`) with correct `01_Project/AvidMXFPeek/Resources` path, otool-based dylib preflight that rejects Homebrew ffprobe, and BSD-portable same-file copy guard. Rewrote `sign-bundled-binaries.sh` — stripped dylib loop and legacy-binary signing block (both obsolete post-Wave-4.6-A), down to 55 lines. Deleted `bundle-ffmpeg.sh`. Both scripts smoke-tested green: ffprobe 8.1 verified, dylib check passes on martin-riedl.de static arm64, codesign outputs `flags=0x10000(runtime) team=FDMSRXXN73`. P5 done. Two sprint items remain: 5.2b (user-gated), 5.3 (code-testable anytime — next). |
