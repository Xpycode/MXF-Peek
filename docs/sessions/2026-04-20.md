# Session: 2026-04-20

## Goal
Stand up the new project (working name "AvidAudit", later renamed to "Avid MXF Peek" this same session) — macOS auditor for Avid `MediaFiles/MXF/*/` folders, forked conceptually from P2toMXF. Set up Directions documentation framework and folder structure before any code.

## Context
- User found a Reddit thread (r/Avid, 2026-04-20) about the pain of auditing Avid media: Kyno is dying, Media Tool is clunky, MDVx only reads databases, Resolve handles preview but not orphan detection.
- Earlier today in P2toMXF, a feasibility session was logged at `P2toMXF/docs/sessions/2026-04-20.md` concluding ~60% reusable and recommending a fork (not an extend).
- This session: scaffold the new project per that handoff.

## Actions
1. Read `P2toMXF/docs/sessions/2026-04-20.md` — the handoff log with reuse checklist and v1 scope.
2. Copied Directions template from `/Users/sim/XcodeProjects/0-DIRECTIONS/__DIRECTIONS/*` into `AvidAudit/docs/`.
3. Created numbered folder structure: `01_Project`, `02_Design/Exports`, `03_Screenshots`, `04_Exports`.
4. Copied `.gitignore` from P2toMXF (same Xcode/macOS pattern).
5. Created working files:
   - `docs/PROJECT_STATE.md` — phase, v1 scope draft, open questions
   - `docs/decisions.md` — 3 seed decisions (fork-don't-extend, read-only v1, `mxf2raw --info` for UMIDs)
   - `docs/sessions/_index.md` — session index
   - `CLAUDE.md` — project root AI context with reuse checklist pointing at P2toMXF files
6. No code changes.
7. **Folder rename.** After the five open questions were resolved, user asked to rename in-session. Executed `mv /Users/sim/XcodeProjects/1-macOS/AvidAudit /Users/sim/XcodeProjects/1-macOS/AvidMXFPeek` from a parent-dir invocation. Same-filesystem rename = inode-level operation on macOS, so the shell CWD (PID 45764) and all Node/Python/uv processes that had their `cwd` pointed at the old path continued working without restart. Patched all forward-looking `AvidAudit` references across `CLAUDE.md`, `docs/PROJECT_STATE.md`, `docs/decisions.md`, and this log; preserved historical `AvidAudit` references in context/options-considered sections.

## Decisions logged (see `docs/decisions.md`)
- Fork from P2toMXF rather than extend it
- v1 is read-only (no writes, no preview, no DB integration)
- Use bundled `mxf2raw --info` for UMID extraction (no custom MXF parser)

## Open questions — resolved
All five locked in this session:
1. **App name** — "Avid MXF Peek" (display) / `AvidMXFPeek` (target/scheme/executable)
2. **Bundle ID** — `com.lucesumbrarum.AvidMXFPeek` (matches CropBatch convention; P2toMXF's `com.p2tomxf.app` is the outlier)
3. **Scope UX** — single-folder drop target (no library, no persisted scan history)
4. **Export in v1** — yes, both CSV and JSON in v1 (not deferred)
5. **`.mdb`/`.pmr` parsing** — needs proper research; runs as a parallel spike (non-blocking for app shell + grouping + export)

## Next session
Folder already renamed, so straight to code scaffolding:
1. Read `docs/cookbook/00-app-shell.md` — mandatory per global CLAUDE.md.
2. Create the Xcode project in `01_Project/AvidMXFPeek.xcodeproj` using the App Shell Standard: HSplitView + FCPToolbarButtonStyle + Theme + `.windowStyle(.hiddenTitleBar)` + `.preferredColorScheme(.dark)`.
   - `CFBundleDisplayName` = `Avid MXF Peek`, `CFBundleName` = `AvidMXFPeek`, bundle ID = `com.lucesumbrarum.AvidMXFPeek`
3. Port `Resources/lib/` + `bundle-ffmpeg.sh` + `sign-bundled-binaries.sh` from P2toMXF. Remember: `Resources/lib/` must NOT be in the Xcode project navigator — it's ditto'd in by a Run Script build phase; also `ENABLE_USER_SCRIPT_SANDBOXING=NO`.
4. Port `BMXWrapper.swift` and extend with `info(url:) async throws -> MXFHeaderInfo` that parses `MaterialPackageUID` / `SourcePackageID` / project name from `mxf2raw --info` output.
5. Port directory-scan pattern from `P2CardParser.discoverP2Cards(in:)` → `withTaskGroup`-based parallel scan.
6. **First milestone:** pick folder → list all MXFs with their `MaterialPackageUID`. Grouping + report export come after that works.
7. **In parallel:** kick off `.mdb`/`.pmr` spike. Capture sample files from a known-state MediaFiles folder, survey MDVx / `mdbtool` / Avid forums, hex-inspect for record structure. Target: enough understanding to read clip-reference records keyed by UMID.
