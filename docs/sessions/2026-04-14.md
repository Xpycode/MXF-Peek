# Session: 2026-04-14

## Goal
Rename root dev folder `/Users/sim/XcodeProjects` → `/Users/sim/CodingProjects` to better reflect that most work now happens in Warp/Claude Code (not just Xcode), and that the tree also holds web, iOS, and other non-Xcode projects.

## Context
- Trigger: folder name undersells what's actually there
- Risk surface: 75 per-project entries in `~/.claude.json`, 46 Claude session dirs, 4 project CLAUDE.md files, plus user-level config

## Progress

### Completed
- [x] Scoped every reference to `/Users/sim/XcodeProjects` across user config, project tree, and shell
- [x] Wrote idempotent rename script: `/Users/sim/rename-to-codingprojects.sh`
  - Pre-flight: refuses to run if cwd is inside the tree, if any `claude`/`Xcode` process is alive, or if any process has the old path in its command line
  - Backs up `.claude.json`, `.claude/CLAUDE.md`, `.claude/settings.json`, `.zshrc`, and 4 project CLAUDE.md files to `~/.rename-backup-<ts>/`
  - Renames folder, renames 46 `~/.claude/projects/-Users-sim-XcodeProjects*` session dirs in lockstep, creates safety symlink old→new
  - Patches all config files with sed, validates JSON after each
  - Prints rollback commands in final output

### Decisions Made
- **Name:** `CodingProjects` (capitalized, no space — shell-friendly, explicit)
- **Symlink strategy:** keep `XcodeProjects → CodingProjects` long-term as safety net for any reference we missed (Xcode recents, tool caches, etc.). Zero cost.
- **Claude session dirs in sync:** rename alongside the folder so per-project conversation history + memory continues to resolve in the new cwd
- **Script runs outside Claude:** user must close all Claude sessions, Xcode, and the running `astro preview` under `4-OTHER/2110/` before executing. Script enforces this.

### Discovered
- `~/.claude.json` stores a per-project DB keyed on absolute cwd — 75 entries under `XcodeProjects/**`. Missing this would cause every project to appear untrusted / re-auth MCP servers in the new location
- Xcode projects themselves are path-independent (relative paths in `.xcodeproj`, UUID-keyed `DerivedData`) — builds unaffected
- No hits in `.xcconfig`, `.pbxproj`, `.sh`, `.command`, or `.mcp.json` files → renaming won't break any build scripts or MCP configs
- Project-internal CLAUDE.md files with hardcoded paths: only 4 of ~47 — `1-macOS/{SxStoMXF,Directions,P2toMXF}`, `3-Websites/ProjectOverview`

### Blockers
- None — script is ready, awaiting user execution after closing sessions

## Next Session
- Run `~/rename-to-codingprojects.sh` from `~`
- Start fresh Claude session in `/Users/sim/CodingProjects`, verify:
  - `/status` works, previous memory recalled (confirms session-dir rename)
  - Open one Xcode project, build once
  - `$PREVIEW_BUILD_PATH` resolves (confirms `~/.zshrc` patch)
- Optional follow-up: sweep project-level `.claude/settings.local.json` files for stale allow-list entries referencing old path
- Consider updating `~/.claude/CLAUDE.md` header text (references "XcodeProjects" conceptually in a few places beyond paths)

## Notes
- Rollback commands included in script output; backup dir survives the rename
- Symlink means even a partial failure is recoverable — anything reading the old path keeps working
