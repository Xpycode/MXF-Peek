# Session: 2026-04-19

## Goal
Persist project-identity conventions (Apple Team ID + canonical bundle-ID pattern) for future sessions, and scan existing macOS apps for other recurring signing/project settings worth formalizing.

## Context
- Previous session: [2026-04-14](2026-04-14.md) — folder rename scoping
- Current phase: maintenance / knowledge capture
- Trigger: user-supplied facts (Team ID `FDMSRXXN73`, bundle ID pattern `com.lucesumbrarum.<AppName>`) plus request to generalize

## Progress

### Completed
- [x] Saved auto-memory: `memory/project_identity_conventions.md` (type: user) + index pointer in `MEMORY.md`
- [x] Added Directions reference doc: `__DIRECTIONS/14_project-identity.md` (slots into the 10–13 setup-docs cluster)
- [x] Scanned 8 active macOS `project.pbxproj` files to confirm which patterns are actually in use vs. aspirational

### Discovered

**Bundle-ID patterns in active use (two, not one):**
- Canonical (user's stated pattern): `com.lucesumbrarum.<AppName>` — confirmed on DesktopStats
- Legacy (capitalized, no reverse-DNS): `LucesUmbrarum.<AppName>` — Penumbra, LogCountdowner, ProjectProgressTracker
- Decision: do NOT migrate legacy apps unilaterally — bundle-ID changes break Sparkle feeds, Keychain, sandbox containers, App Store linkage

**Team ID wiring status:**
- `DEVELOPMENT_TEAM = FDMSRXXN73` set on: Penumbra, Directions, ProjectProgressTracker
- Still `""` or placeholder on: DesktopStats, TimeCodeEditor, ClipSmart, ViME, FCPWorkspaceEditor — flag before signing/shipping

**Recurring settings worth formalizing as defaults:**
- `CODE_SIGN_STYLE = Automatic`
- `ENABLE_HARDENED_RUNTIME = YES`
- `ENABLE_APP_SANDBOX = YES` (App Store targets)
- `GENERATE_INFOPLIST_FILE = YES`
- `INFOPLIST_KEY_NSHumanReadableCopyright = "Copyright © <YEAR> Luces Umbrarum. All rights reserved."`
- `INFOPLIST_KEY_LSApplicationCategoryType` — utilities or video depending on app
- Swift 6.0 on new apps; 5.0 on older (no blanket migration)

**Non-patterns (per-app decisions, no canonical value):**
- `MACOSX_DEPLOYMENT_TARGET` — ranges 13.0 → 26.0 across apps; Penumbra deliberately on 26.0 (Tahoe-only)
- Category type — depends on app purpose

### Decisions Made
- MEMORY.md kept flat for existing sections but new entry added as one-line index pointer (matching CLAUDE.md's stated index convention)
- Legacy `LucesUmbrarum.<AppName>` bundle IDs stay untouched — documented as "intentional legacy" not "needs migration"
- Identity reference lives at `__DIRECTIONS/14_project-identity.md` (slots after `13_folder-structure.md`, fits the setup cluster)

### Blockers
- None

## Next Session
- If user scaffolds a new macOS app, pre-fill Team ID + canonical bundle ID from the start (no placeholders)
- Optional: sweep DesktopStats, TimeCodeEditor, ClipSmart, ViME, FCPWorkspaceEditor to wire up `DEVELOPMENT_TEAM = FDMSRXXN73` and replace placeholder bundle IDs when those apps are ready to sign

## Notes
- Memory deliberately includes a "Verify before asserting" note — Xcode settings drift, and stale memory about `DEVELOPMENT_TEAM` values would be actively misleading
